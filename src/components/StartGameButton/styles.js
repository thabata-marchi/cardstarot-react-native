import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  btnPlay: {
    backgroundColor: '#C00',
    borderRadius: 50,
    width:50,
    height:50,
    marginBottom: 10,  
  },
  play: {
    color: '#fff',
    paddingLeft: 18,
    paddingTop: 15,
    fontSize: 20,
  }
})

export default styles;